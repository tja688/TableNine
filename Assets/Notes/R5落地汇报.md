# R5 落地汇报

> 阶段：R5 — SkillSystem + Relic Runtime  
> 执行时间：2026-06-12  
> 分支：`dev`  
> 状态：✅ 验收通过

---

## 1. 执行摘要

本阶段落地 `ISkillSystem` / `SkillSystem` / `EffectTriggerQueue` 最小触发骨架，将荆棘甲、刺皮、活着的肉等战斗/节点效果从 `CombatSystem` 硬编码迁入技能注册表，并补齐 R4 预留的 **治疗泉**（棋盘相邻回血 + 道具位战后回血）与 **滚石**（移至格3击杀格6非精英怪物）位置被动效果。木制套装加成、硬皮减伤、木盾节点护甲同步均有回归。Unity 编译 0 error，R5 专项 14/14、NewRuleTests 65/65 通过。

---

## 2. 文件变更清单（质检入口）

### 2.1 新增

| 路径 | 说明 |
|------|------|
| `Assets/Scripts/Skill/SkillRuntimeData.cs` | `SkillTrigger`、`TriggerContext`、`SkillTriggerLog`、`CardMovedEvent` |
| `Assets/Scripts/Skill/SkillEffectRegistry.cs` | 遗物/技能/帮助卡被动绑定表 + 被动效果图 |
| `Assets/Scripts/System/SkillSystem.cs` | `ISkillSystem`、触发队列、条件评估、调用 `IEffectSystem` |
| `Assets/Scripts/Command/SkillTriggerCommands.cs` | `TriggerSkillSystemCommand` |
| `Assets/Scripts/Tests/EditMode/TableNineR5SkillSystemEditModeTests.cs` | R5 专项 14 条（含治疗泉/滚石/荆棘/套装） |

### 2.2 修改

| 路径 | 改动要点 |
|------|----------|
| `Assets/Scripts/TableNine.cs` | 注册 `ISkillSystem`；根 Command 入口重置触发队列 |
| `Assets/Scripts/System/RuntimeSystems.cs` | `BoardSystem` 发 `CardMovedEvent`；并行伤害改走 SkillSystem；木套装/硬皮 |
| `Assets/Scripts/System/EffectSystem.cs` | `RemoveCard` 支持 `board_slot_monster` 作用域（滚石） |
| `Assets/Scripts/Command/RuntimeCommands.cs` | 节点开始/战后/用卡触发 SkillSystem |
| `Assets/Scripts/Tests/EditMode/TableNineR0BaselineEditModeTests.cs` | 基线断言 SkillSystem 已注册 |

### 2.3 删除

无。

---

## 3. Plan 任务对照

来源：[Plan.md §7 R5](Plan.md)

| 任务项 | 状态 | 落点 |
|--------|------|------|
| R5-1 SkillSystem 最小骨架 | ✅ | `SkillSystem.cs`、`EffectTriggerQueue` |
| R5-1 触发后调用 EffectSystem | ✅ | 被动效果图经 `IEffectSystem.ResolveAtom` |
| R5-1 防递归 | ✅ | 根 Command 重置 + 深度上限 8 + 同键去重 |
| R5-2 刺皮迁入统一系统 | ✅ | `SkillEffectRegistry` + `CollectParallelDamage` |
| R5-2 硬皮减伤 | ✅ | `StatSystem.GetEffectiveMonsterStats` |
| R5-3 荆棘甲并行伤害 | ✅ | 自 `CombatSystem` 迁至 SkillSystem |
| R5-3 凤凰羽毛 | ✅ | 保留 `ApplyDeathPreventCommand`（R3），R5 回归通过 |
| R5-3 木剑/木盾/木甲 + 套装 | ✅ | `StatSystem.HasWoodSet` |
| R5-3 活着的肉 | ✅ | `OnHelpCardUsed` → 回血 1 |
| R5-4 导师技能收集 | ⚠️ | 玩家技能已在 `playerModel.SkillIds` 收集；配置 trigger 字段未扩展 |
| **R4 延期：治疗泉/滚石** | ✅ | `OnCardMoved` / `OnAfterCombat` 被动绑定 |

### 验收项

| 验收条件 | 结果 | 证据 |
|----------|------|------|
| 荆棘甲战斗额外伤害 | 通过 | `Thorn_Armor_Still_Parallels_Player_First_Hit_Via_SkillSystem` |
| 凤凰羽毛致命伤免死 | 通过 | `Phoenix_Feather_Still_Prevents_Lethal_Death` |
| 木盾节点护甲增加 | 通过 | `Wood_Shield_Increases_Node_Start_Armor` |
| 刺皮反击同步反伤 | 通过 | `Thorn_Skin_Still_Parallels_Monster_Counter_Via_SkillSystem` |
| 触发日志含 owner/trigger/graph | 通过 | `SkillSystem_Logs_Trigger_With_Owner_And_EffectGraph` |
| 防递归上限 | 通过 | `SkillSystem_Blocks_Repeated_Same_Trigger_In_Same_Pass` |
| 治疗泉相邻回血 | 通过 | `Healing_Spring_Heals_When_Placed_Adjacent_To_Player` |
| 滚石格3击杀格6怪 | 通过 | `Boulder_Removes_Non_Elite_Monster_At_Slot_6_When_Moved_To_Slot_3` |

---

## 4. 架构符合性

- **SkillSystem 职责**：收集绑定、评估条件、防递归、派发 EffectSystem / 并行伤害，符合 ArchitectureDesign §6.7。
- **状态变更仍走 Command**：被动回血/移除/消耗经既有 `EffectAtomCommands`。
- **QFramework 约束**：`TriggerSkillSystemCommand` 作为 `ICanSendCommand` 入口；System 内通过 `IBelongToArchitecture` 派发子 Command。

---

## 5. 测试与 Console 证据

```
EditMode（R5 专项）：14/14 通过
EditMode（NewRuleTests）：65/65 通过
PlayMode：N/A
Unity Console：0 compile error；既有 monster_*_l2 配置 warning
验证方式：Unity MCP（run_tests + read_console）
```

**本阶段新增/改动测试：**

- `Healing_Spring_Heals_When_Placed_Adjacent_To_Player` — 治疗泉移至玩家相邻格 +2 HP
- `Healing_Spring_In_Item_Slot_Heals_After_Combat` — 道具位治疗泉战后 +1 HP
- `Boulder_Removes_Non_Elite_Monster_At_Slot_6_When_Moved_To_Slot_3` — 滚石格3触发击杀
- `Boulder_Does_Not_Remove_Elite_Monster_At_Slot_6` — 精英免疫
- `Wood_Set_Grants_Extra_Stats` — 三件套额外 +2/+2/+8

**建议质检优先跑：**

```text
TableNineR5SkillSystemEditModeTests
TableNineR3CombatPipelineEditModeTests
Category: NewRuleTests
```

---

## 6. 质检路线图

1. **核心逻辑**：`Assets/Scripts/System/SkillSystem.cs` — `Trigger`、`CollectParallelDamage`、条件键
2. **注册表**：`Assets/Scripts/Skill/SkillEffectRegistry.cs` — 荆棘/刺皮/治疗泉/滚石绑定
3. **棋盘移动**：`Assets/Scripts/System/RuntimeSystems.cs` — `CardMovedEvent` 发射点
4. **测试**：`TableNineR5SkillSystemEditModeTests.cs`
5. **已知排除项**：复仇/红桃之母/四幼崽位置技能仍主要在 StatSystem；瞭望塔/倍增塔持续效果未做；导师技能配置 trigger 字段未扩展

---

## 7. 风险、假设与后续

### 实现假设（待产品/主程确认）

- 治疗泉/滚石被动通过 `SkillEffectRegistry` + `OnCardMoved` 实现，点击消耗仍走 R4 `EffectGraphRegistry` 的 `Consume()`。
- 滚石仅排除 Elite/Boss，Level4 普通怪仍可击杀。
- 木制套装加成在 `StatSystem` 三件齐全时额外 +2 攻/+2 防/+8 生命（叠在单件属性之上）。

### 已知问题 / 技术债

- `SkillDefinition` 尚未增加 `EffectGraphId`/`Trigger` 配置字段，绑定表仍为 C# 注册。
- 凤凰羽毛仍由 `ApplyDeathPreventCommand` 处理，未迁入 SkillSystem 绑定（行为已回归）。

### 对下一阶段的建议

- R6 房间流程可在 `OnNodeEnd`/`OnNodeClear` 挂接更多遗物。
- 塔类持续效果可复用 `OnCardMoved` + 计数消耗模式。

---

## 8. 变更统计

```
新增文件：5
修改文件：6
删除文件：0
新增测试：14
```

---

## 质检记录

- **日期**：2026-06-12
- **判定**：❌ 需整改后重新质检
- **核查范围**：汇报 §2 变更清单 + Plan §7 R5 + ArchitectureDesign §1.5 / §6.7
- **验证执行**：EditMode 14/14（`TableNineR5SkillSystemEditModeTests`）、65/65（`NewRuleTests`）；Console 无新增 compile error，存在既有 `monster_*_l2/l3` 配置 warning

### 问题摘要

R5 当前不能归档，主要不是“代码跑不通”，而是 **已通过的回归并没有锁住设计要求**。最严重的问题是 `治疗泉` / `滚石` 的位置被动把“上板/补牌”也当成“移动”，与设计文档的“移动到”语义不一致；现有 R5 测试正是用 `PlaceCard` 直接上板来断言通过，因此把偏差固化成了正确行为。其次，Plan §7 的 `R5-4 导师技能上线` 仍未完成：导师技能配置没有 trigger/effectGraph 入口，`ChooseTutorSkillCommand` 只做 `AddSkill`，而测试只覆盖了手工塞技能或怪物手工挂词条，无法证明导师技能获得后的真实运行时效果。以上问题都跨越配置、运行时和测试证据，整改面已超过“小修归档”范围。

### 符合项（可保留）

- `ISkillSystem` / `SkillSystem` 已注册到 [TableNine.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/TableNine.cs:47)，根 Command 触发深度重置也已接入。
- 荆棘甲并行伤害、凤凰羽毛死亡预防、木制套装加成、木盾节点护甲等主路径回归仍成立。
- `R5` 专项与 `NewRuleTests` 均可在 Unity 中跑通，当前没有新增编译错误。

---

## 整改条目

> 每条须写清 **要求**（规范/设计依据）与 **当前实现现状**（可定位的文件与行为）。  
> 整改完成后由推进代理处理，并重新触发 `qa-phase-rx` 质检。

| # | 类别 | 严重度 | 要求 | 当前实现现状 | 建议落点 |
|---|------|--------|------|--------------|----------|
| 1 | Plan 未达标 | P0 | `Assets/Docs/07-数据/帮助卡数据.md:45-46,90-92` 与 `Assets/Docs/01-机制规则/卡牌移动.md:30-31` 明确要求：`滚石` / `治疗泉` 仅在“移动到”指定位置时触发位置被动；点击仅负责消耗。 | [RuntimeSystems.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/System/RuntimeSystems.cs:123) 在 `PlaceCard` 时无论是开局发牌、补牌还是真实位移都会发送 `CardMovedEvent(uid, slot, null, source)`；[SkillSystem.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/System/SkillSystem.cs:287) 的 `moved_to_adjacent_player` / `moved_to_slot_3_killable` 只看 `CardSlot/PreviousSlot`，完全不区分 `OpeningHelp` / `Refill` / 真正移动；对应 R5 测试 [TableNineR5SkillSystemEditModeTests.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/Tests/EditMode/TableNineR5SkillSystemEditModeTests.cs:22) 与 [TableNineR5SkillSystemEditModeTests.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/Tests/EditMode/TableNineR5SkillSystemEditModeTests.cs:57) 也是通过 `PlaceCard` 直接上板来断言通过，已把错误语义写进回归。 | `CardMovedEvent` / `TriggerContext` 补充位移动因，收紧 `BoardSystem.PlaceCard` 与被动条件判断；同步重写 `治疗泉` / `滚石` 用例，区分“上板/补牌”与“真实移动”。 |
| 2 | Plan 未达标 | P0 | `Assets/Notes/Plan.md:803-807` 的 `R5-4 导师技能上线` 要求：技能配置有 trigger，获得后能在 `SkillSystem` 中被收集，重复获取按规则处理，UI 可显示生效描述。`Assets/Docs/04-技能/玩家技能.md:15-20` 还定义了 `刺皮` / `硬皮` 的玩家技能语义。 | [GameRuntimeData.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/Data/GameRuntimeData.cs:330) 的 `SkillDefinition` 仍只有 `SkillId/DisplayName/GrantsFirstStrike`，没有 trigger/effectGraph/stackPolicy；[RuntimeCommands.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/Command/RuntimeCommands.cs:1831) 的 `ChooseTutorSkillCommand` 只做 `playerModel.AddSkill`；`TutorSkillPoolIds` 仍把 `skill_thorn_skin` / `skill_hard_skin` 暴露给导师池，但当前 R5 只在 [SkillEffectRegistry.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/Skill/SkillEffectRegistry.cs:20) 硬编码了 `刺皮` 的单条绑定，`硬皮` 则只在 [RuntimeSystems.cs](/C:/Users/jinji/Documents/GitHub/TableNine/Assets/Scripts/System/RuntimeSystems.cs:570) 作为怪物减伤处理。现有专项测试分别通过 `playerModel.AddSkill(...)` 和手工给怪物挂词条来验证，不能证明“导师技能获得后立即生效”。 | 补 `SkillDefinition` 配置入口，打通 `ChooseTutorSkillCommand -> SkillSystem/StatSystem` 的真实链路，并新增“选择导师技能后生效”的集成测试。 |
| 3 | 测试缺口 | P1 | `qa-phase-rx` 质检基准要求“汇报声称的 EditMode 要真实覆盖规则，而不是只测工程通路”。 | R5 用例没有覆盖“补牌到相邻格/3号位时不应触发位置被动”“选择 `刺皮/硬皮` 导师技能后的真实效果”“技能配置缺字段时的降级/报错”。当前 14/14 与 65/65 只能证明现实现状一致，不能证明符合设计目标。 | 扩充 `TableNineR5SkillSystemEditModeTests`，增加位置被动负例、导师技能正例、配置约束测试。 |

**严重度**：P0 = 阻塞下一阶段；P1 = 本阶段应修；P2 = 可记入技术债但需标注。

### Plan 验收复验表（整改后必填）

| 验收条件 | 整改前 | 整改后（返工时填） |
|----------|--------|-------------------|
| `R5-4` 导师技能获得后能在 `SkillSystem` 中被收集并生效 | ❌ | |
| `治疗泉` / `滚石` 位置被动符合设计文档“移动到”语义 | ❌ | |
| R5 专项测试能真实锁住上述规则，而非只验证当前实现 | ❌ | |

### 建议返工顺序

1. 先修 `CardMovedEvent` / `SkillSystem` 的位置触发语义，避免继续把“补牌即移动”扩散到后续塔类效果。
2. 再补 `SkillDefinition` 与 `ChooseTutorSkillCommand` 的导师技能主链，把 `刺皮` / `硬皮` 等技能从“手工塞数据可工作”收束成“真实获得即可工作”。
3. 最后重写 R5 专项测试与相关汇报证据，确认通过后再重新质检。

### 重新质检入口

整改完成后，确认以下文件/测试无回归，再执行「质检 R5」：

- 核心：`Assets/Scripts/System/RuntimeSystems.cs`、`Assets/Scripts/System/SkillSystem.cs`、`Assets/Scripts/Skill/SkillRuntimeData.cs`、`Assets/Scripts/Skill/SkillEffectRegistry.cs`、`Assets/Scripts/Command/RuntimeCommands.cs`、`Assets/Scripts/Data/GameRuntimeData.cs`
- 测试：`TableNineR5SkillSystemEditModeTests`
