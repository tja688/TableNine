interface:
  display_name: "Game Character Sprites"
  short_description: "Make reference-grounded 32/64/128 pixel sprite animations."
  default_prompt: "Use $game-character-sprites to create fixed-cell pixel-art character animations with separate imagegen outputs for each requested native cell size, provenance, visual QA, and GIF/WebP previews."
